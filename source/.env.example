# Entorno general
NODE_ENV=development
APP_URL=http://localhost:3000
API_URL=http://localhost:4000
WEB_HOST=0.0.0.0
WEB_PORT=3000
API_HOST=0.0.0.0
API_PORT=4000
API_INTERNAL_URL=http://localhost:4000

# PostgreSQL
POSTGRES_USER=atelier
POSTGRES_PASSWORD=CAMBIAR_EN_EL_SERVIDOR
POSTGRES_DB=atelier_lumiere
DATABASE_URL=postgresql://atelier:CAMBIAR_EN_EL_SERVIDOR@database:5432/atelier_lumiere
DATABASE_POOL_MAX=10
DATABASE_STATEMENT_TIMEOUT_MS=5000

# Sesiones y autenticación definitiva (bloque posterior)
AUTH_SECRET=GENERAR_CLAVE_LARGA_EN_EL_MINI_PC
AUTH_TRUSTED_ORIGINS=http://localhost:3000

# Acceso administrativo temporal para pruebas privadas
# Ambos interruptores deben permanecer en false hasta configurar el mini PC.
# La API temporal nunca funciona con NODE_ENV=production.
ALLOW_DEV_ADMIN_AUTH=false
ENABLE_ADMIN_UI=false
DEV_ADMIN_TOKEN=GENERAR_TOKEN_ALEATORIO_DE_AL_MENOS_32_CARACTERES
DEV_ADMIN_USER_ID=00000000-0000-4000-8000-000000000001
DEV_ADMIN_EMAIL=admin@atelier.localhost
DEV_ADMIN_DISPLAY_NAME=Administración Atelier Lumière
WEB_ADMIN_ACCESS_KEY=GENERAR_CLAVE_PRIVADA_DE_AL_MENOS_24_CARACTERES
WEB_ADMIN_SESSION_TTL_MINUTES=480
# Activar cuando el panel se publique exclusivamente mediante HTTPS.
WEB_COOKIE_SECURE=false

# Correo de invitaciones y verificación
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=correo@example.com
SMTP_PASSWORD=CAMBIAR_EN_EL_SERVIDOR
SMTP_FROM=Atelier Lumière <correo@example.com>
EMAIL_VERIFICATION_TTL_HOURS=24
EMAIL_VERIFICATION_RESEND_COOLDOWN_SECONDS=60

# Almacenamiento multimedia
STORAGE_DRIVER=local
STORAGE_PATH=/data/media
MAX_IMAGE_MB=12
MAX_VIDEO_MB=50
MAX_IMAGES_PER_PRODUCT=8

# Seguridad
REQUIRE_PROVIDER_2FA=true
INVITATION_TTL_HOURS=48
