import assert from "node:assert/strict";
import { readFile } from "node:fs/promises";
const paths=["packages/database/migrations/0024_customer_order_access.sql","apps/api/src/customer-auth-service.mjs","apps/api/src/customer-orders-service.mjs","apps/api/src/customer-orders-api.mjs","apps/api/src/server.mjs","apps/web/src/customer-orders-proxy.mjs","apps/web/src/server.mjs","apps/web/public/pedido/acceso/index.html","apps/web/public/pedido/acceso/access.js","apps/web/public/mis-pedidos/index.html","apps/web/public/mis-pedidos/orders.js","apps/web/public/mis-pedidos/detalle/index.html","apps/web/public/mis-pedidos/detalle/order-detail.js","apps/web/public/mis-pedidos/encargo/index.html","apps/web/public/mis-pedidos/encargo/request-detail.js","apps/web/public/mis-pedidos/common.js"];
const files=Object.fromEntries(await Promise.all(paths.map(async path=>[path,await readFile(new URL(`../${path}`,import.meta.url),"utf8")])));
const migration=files[paths[0]],auth=files[paths[1]],service=files[paths[2]],api=files[paths[3]],apiServer=files[paths[4]],proxy=files[paths[5]],webServer=files[paths[6]],access=files[paths[8]];
assert.match(migration,/customer_order_access_tokens/);assert.match(migration,/customer_sessions/);assert.match(migration,/token_hash text NOT NULL UNIQUE/);assert.match(migration,/consumed_at/);assert.match(migration,/revoked_at/);assert.match(migration,/FORCE ROW LEVEL SECURITY/);assert.doesNotMatch(migration,/access_token text|session_token text/i);
assert.match(auth,/randomBytes\(32\)\.toString\("base64url"\)/);assert.match(auth,/sha256\(accessToken\)/);assert.match(auth,/FOR UPDATE OF access/);assert.match(auth,/consumed_at = now/);assert.match(auth,/UPDATE customer_sessions[\s\S]*revoked_at = now/);
assert.match(service,/role !== "CUSTOMER"/);assert.match(service,/customer_user_id = \$2/);assert.match(service,/status = 'QUOTED' AND version = \$3/);assert.match(service,/CUSTOM_REQUEST_VERSION_CONFLICT/);assert.match(service,/status = 'PENDING_CONFIRMATION'/);
assert.match(api,/\/api\\\/customer\\\/access\\\/consume/);assert.match(api,/customerAuthService\.authenticate/);assert.match(api,/\/approve/);assert.match(apiServer,/createCustomerOrdersApiHandler/);
assert.match(proxy,/atelier_customer_session/);assert.match(proxy,/HttpOnly/);assert.match(proxy,/SameSite=Strict/);assert.match(proxy,/payload\.sessionToken/);assert.doesNotMatch(proxy,/sessionToken: payload\.sessionToken/);assert.match(webServer,/createCustomerOrdersWebHandler/);
assert.match(access,/window\.location\.hash/);assert.match(access,/history\.replaceState/);assert.match(access,/\/internal\/customer\/access/);assert.doesNotMatch(access,/console\.|localStorage|sessionStorage|Authorization|Bearer/);
for(const path of paths.slice(7)){const content=files[path];if(path.endsWith(".html")){assert.match(content,/noindex,nofollow,noarchive/);assert.doesNotMatch(content,/\sstyle=/i);assert.doesNotMatch(content,/<script[^>]*>[^<]/i)}else{assert.doesNotMatch(content,/innerHTML|Authorization|Bearer|localStorage|sessionStorage/);assert.match(content,/textContent|replaceChildren|history\.replaceState/)}}
assert.match(files[paths[12]],/expectedVersion:detail\.order\.version/);assert.match(files[paths[14]],/expectedVersion:detail\.request\.version/);assert.match(files[paths[14]],/\/approve/);
console.log("Acceso privado y pedidos del cliente validados.");
