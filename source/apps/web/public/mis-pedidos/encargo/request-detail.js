(() => {
  const {REQUEST_LABELS,byId,element,money,date,badge,requestJson,keyValue,queryUuid,setMessage,wireLogout}=window.AtelierCustomerOrders;
  const requestId=queryUuid("id");let detail=null;
  function messageNode(message){const customer=message.authorRole==="CUSTOMER";const node=element("article",`bubble${customer?" customer":""}`);node.append(element("p","",message.body),element("small","",`${customer?"Tú":"Taller"} · ${date(message.createdAt,true)}`));return node}
  function fileNode(file){const row=element("article","compact-row");const header=element("header");header.append(element("h3","",file.originalFilename),element("strong","",file.status));row.append(header,element("p","",`${file.mimeType} · ${new Intl.NumberFormat("es-ES").format(file.sizeBytes)} bytes · ${date(file.createdAt)}`));if(file.rejectionReason)row.append(element("p","",`Motivo: ${file.rejectionReason}`));return row}
  function render(){const request=detail.request;byId("provider-name").textContent=request.providerName;byId("order-number").textContent=request.orderNumber;byId("request-title").textContent=request.title;byId("request-status").replaceChildren(badge(request.status,"request"));byId("request-brief").textContent=request.brief;byId("request-quote").textContent=request.quotedPriceCents===null?"Presupuesto pendiente":money(request.quotedPriceCents,request.currency);byId("messages-list").replaceChildren(...detail.messages.map(messageNode));byId("messages-empty").hidden=detail.messages.length!==0;byId("files-list").replaceChildren(...detail.files.map(fileNode));byId("files-empty").hidden=detail.files.length!==0;byId("request-data").replaceChildren(keyValue("Taller",request.providerName),keyValue("Pedido",request.orderNumber),keyValue("Estado",REQUEST_LABELS[request.status]??request.status),keyValue("Fecha deseada",request.desiredDate?date(request.desiredDate):"No indicada"),keyValue("Presupuesto",request.quotedPriceCents===null?"Pendiente":money(request.quotedPriceCents,request.currency)),keyValue("Versión",`v${request.version}`));byId("order-link").href=`/mis-pedidos/detalle/?id=${encodeURIComponent(request.orderId)}`;byId("approval-card").hidden=request.status!=="QUOTED";byId("approved-card").hidden=!["APPROVED","IN_PROGRESS","COMPLETED"].includes(request.status);if(request.status==="QUOTED"){byId("approval-quote").replaceChildren(element("span","","Importe propuesto por el taller"),element("strong","",money(request.quotedPriceCents,request.currency)),element("small","","Esta aprobación no realiza todavía ningún cobro."))}byId("message-body").value=""}
  async function load(){if(!requestId){byId("detail-loading").hidden=true;byId("detail-error-message").textContent="El identificador no es válido.";byId("detail-error").hidden=false;return}try{detail=await requestJson(`/internal/customer/custom-requests/${encodeURIComponent(requestId)}`);render();byId("detail-loading").hidden=true;byId("detail-content").hidden=false}catch(error){byId("detail-loading").hidden=true;byId("detail-error-message").textContent=error.message;byId("detail-error").hidden=false}}
  byId("message-form").addEventListener("submit",async event=>{event.preventDefault();const button=byId("message-button"),result=byId("message-result");button.disabled=true;button.textContent="Enviando…";setMessage(result,"");try{await requestJson(`/internal/customer/custom-requests/${encodeURIComponent(requestId)}/messages`,{method:"POST",body:JSON.stringify({body:byId("message-body").value.trim()})});detail=await requestJson(`/internal/customer/custom-requests/${encodeURIComponent(requestId)}`);render();setMessage(result,"Mensaje enviado al taller.","success")}catch(error){setMessage(result,error.message,"error")}finally{button.disabled=false;button.textContent="Enviar mensaje"}});
  byId("approve-button").addEventListener("click",async()=>{const button=byId("approve-button"),result=byId("approve-result");button.disabled=true;button.textContent="Aprobando…";setMessage(result,"");try{await requestJson(`/internal/customer/custom-requests/${encodeURIComponent(requestId)}/approve`,{method:"POST",body:JSON.stringify({expectedVersion:detail.request.version})});detail=await requestJson(`/internal/customer/custom-requests/${encodeURIComponent(requestId)}`);render();setMessage(result,"Presupuesto aprobado. El taller ya puede continuar.","success")}catch(error){setMessage(result,error.message,error.code?.includes("CONFLICT")||error.code?.includes("QUOTED")?"warning":"error")}finally{button.textContent="Aprobar presupuesto";button.disabled=detail?.request?.status!=="QUOTED"}});
  wireLogout();void load();
})();
