(() => {
  const {ORDER_LABELS,byId,element,money,date,badge,requestJson,fact,keyValue,queryUuid,setMessage,wireLogout}=window.AtelierCustomerOrders;
  const orderId=queryUuid("id");let detail=null;
  const EVENT_LABELS={ORDER_CREATED:"Pedido recibido",ORDER_STATUS_ACCEPTED:"Pedido aceptado",ORDER_STATUS_IN_PRODUCTION:"Elaboración iniciada",ORDER_STATUS_READY_TO_SHIP:"Listo para enviar",ORDER_STATUS_SHIPPED:"Pedido enviado",ORDER_STATUS_DELIVERED:"Pedido entregado",ORDER_STATUS_INCIDENT:"Incidencia registrada",ORDER_STATUS_CANCELLED:"Pedido cancelado",PROVIDER_NOTE:"Nota del taller",CUSTOM_REQUEST_STATUS_APPROVED:"Presupuesto aprobado"};
  function itemNode(item){const row=element("article","item-row");const header=element("header");header.append(element("h3","",item.productName),element("strong","price",money(item.lineTotalCents,item.currency)));row.append(header,element("p","",`${item.quantity} × ${money(item.unitPriceCents,item.currency)}${item.story?` · ${item.story}`:""}`));const entries=item.personalization&&typeof item.personalization==="object"?Object.entries(item.personalization):[];if(entries.length){const facts=element("div","personalization");for(const [key,value] of entries)facts.append(fact(`${key}: ${String(value)}`));row.append(facts)}return row}
  function requestNode(request){const row=element("article","compact-row");const header=element("header");const title=element("div");title.append(element("h3","",request.title),badge(request.status,"request"));const link=element("a","button primary",request.status==="QUOTED"?"Revisar presupuesto":"Abrir conversación");link.href=`/mis-pedidos/encargo/?id=${encodeURIComponent(request.id)}`;header.append(title,link);row.append(header,element("p","",`${request.messageCount} mensajes · ${request.fileCount} archivos${request.quotedPriceCents===null?"":` · ${money(request.quotedPriceCents,request.currency)}`}`));return row}
  function eventNode(event){const row=element("article","timeline-item");row.append(element("strong","",EVENT_LABELS[event.type]??event.type.replaceAll("_"," ")),element("small","",`${date(event.createdAt,true)} · ${event.actorRole==="CUSTOMER"?"Cliente":event.actorRole==="SYSTEM"?"Sistema":"Taller"}`));if(event.message)row.append(element("p","",event.message));return row}
  function shipmentNode(shipment){const row=element("article","compact-row");const header=element("header");header.append(element("h3","",shipment.carrier||"Seguimiento"),element("strong","",shipment.status.replaceAll("_"," ")));row.append(header,element("p","",shipment.trackingCode?`Código: ${shipment.trackingCode}`:"Código pendiente"));if(shipment.trackingUrl){const link=element("a","button secondary","Abrir seguimiento");link.href=shipment.trackingUrl;link.target="_blank";link.rel="noopener noreferrer";row.append(link)}return row}
  function incidentNode(incident){const row=element("article","compact-row");const header=element("header");header.append(element("h3","",incident.type.replaceAll("_"," ")),element("strong","",incident.status));row.append(header,element("p","",incident.description));if(incident.resolution)row.append(element("p","",`Resolución: ${incident.resolution}`));return row}
  function render(){const order=detail.order;byId("provider-name").textContent=order.provider.displayName;byId("order-number").textContent=order.orderNumber;byId("order-status").replaceChildren(badge(order.status));byId("order-summary").textContent=`${order.provider.specialty||"Taller artesanal"} · Pedido ${date(order.placedAt)} · Preparación prevista ${order.preparationMinDays??"?"}–${order.preparationMaxDays??"?"} días`;byId("order-total").textContent=money(order.totalCents,order.currency);byId("items-list").replaceChildren(...detail.items.map(itemNode));byId("requests-list").replaceChildren(...detail.customRequests.map(requestNode));byId("requests-empty").hidden=detail.customRequests.length!==0;byId("events-list").replaceChildren(...detail.events.map(eventNode));byId("shipments-list").replaceChildren(...detail.shipments.map(shipmentNode));byId("incidents-list").replaceChildren(...detail.incidents.map(incidentNode));byId("logistics-empty").hidden=detail.shipments.length+detail.incidents.length!==0;byId("order-data").replaceChildren(keyValue("Taller",order.provider.displayName),keyValue("Estado",ORDER_LABELS[order.status]??order.status),keyValue("Artículos",money(order.subtotalCents,order.currency)),keyValue("Envío",money(order.shippingCents,order.currency)),keyValue("Total",money(order.totalCents,order.currency)),keyValue("Nota del cliente",order.customerNote||"Sin observaciones"),keyValue("Nota del taller",order.providerNote||"Sin actualizaciones"));byId("cancel-card").hidden=order.status!=="PENDING_CONFIRMATION"}
  async function load(){if(!orderId){byId("detail-loading").hidden=true;byId("detail-error-message").textContent="El identificador no es válido.";byId("detail-error").hidden=false;return}try{detail=await requestJson(`/internal/customer/orders/${encodeURIComponent(orderId)}`);render();byId("detail-loading").hidden=true;byId("detail-content").hidden=false}catch(error){byId("detail-loading").hidden=true;byId("detail-error-message").textContent=error.message;byId("detail-error").hidden=false}}
  byId("cancel-button").addEventListener("click",async()=>{const button=byId("cancel-button"),result=byId("cancel-result");button.disabled=true;button.textContent="Cancelando…";setMessage(result,"");try{await requestJson(`/internal/customer/orders/${encodeURIComponent(orderId)}/cancel`,{method:"POST",body:JSON.stringify({expectedVersion:detail.order.version})});detail=await requestJson(`/internal/customer/orders/${encodeURIComponent(orderId)}`);render();setMessage(result,"El pedido se ha cancelado.","success")}catch(error){setMessage(result,error.message,error.code?.includes("CONFLICT")?"warning":"error")}finally{button.textContent="Cancelar este pedido";button.disabled=detail?.order?.status!=="PENDING_CONFIRMATION"}});
  wireLogout();void load();
})();
